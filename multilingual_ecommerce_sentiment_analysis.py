import pandas as pd\nfrom textblob import TextBlob\nfrom langdetect import detect\n\n\ndef analyze_sentiment(review):\n    """Analyze the sentiment of a given review and return the polarity and subjectivity."""\n    language = detect(review)\n    blob = TextBlob(review)\n\n    if language != 'en':\n        # Translate to English if the language is not English\n        blob = blob.translate(to='en')\n\n    polarity = blob.sentiment.polarity\n    subjectivity = blob.sentiment.subjectivity\n    return polarity, subjectivity\n\n\ndef process_reviews(reviews):\n    """Process a list of reviews and return their sentiment analysis results."""\n    results = []\n    for review in reviews:\n        polarity, subjectivity = analyze_sentiment(review)\n        results.append({'review': review, 'polarity': polarity, 'subjectivity': subjectivity})\n    return pd.DataFrame(results)\n\n\nif __name__ == '__main__':\n    example_reviews = [\n        "I love this product! It works wonderfully.",\n        "Este producto es terrible y no lo recomendaría a nadie.",\n        "Ce produit est acceptable mais pourrait être amélioré.",\n        "Ich mag dieses Produkt, aber es hat einige Mängel."\n    ]\n    df = process_reviews(example_reviews)\n    print(df)\n